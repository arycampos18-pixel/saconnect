import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default function WAChatbot() {
  return (
    <Card>
      <CardHeader><CardTitle className="text-lg">Chatbot</CardTitle></CardHeader>
      <CardContent className="text-sm text-muted-foreground">Editor de fluxos isolado por empresa — implementação na Fase 3.</CardContent>
    </Card>
  );
}
